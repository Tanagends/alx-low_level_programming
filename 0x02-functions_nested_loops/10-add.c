#include <stdio.h>
#include "main.h"
/**
* add - adds two integers.
* @e: int to be added to f
* @f: int to be added to e
* Return: sum of e and f
*/
int add(int e, int f)
{
	int g;

	g = e + f;
	return (g);
}
