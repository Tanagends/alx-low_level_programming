#include <stdio.h>
/**
 * main - prints alphabet characters.
 * Return: 0(Success)
 */
int main(void)
{
	for (char = a; char <= z; char++)
		if (char == q || char == e)
			continue;
		putchar(char);
	return (0);
}
